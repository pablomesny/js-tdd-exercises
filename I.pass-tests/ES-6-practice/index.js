const { arrow, defaultParameter, combineArrays, destructuring, templateString } = require("./es6");

module.exports = {
    arrow,
    defaultParameter,
    combineArrays,
    destructuring,
    templateString
}